# Server-side only — never expose this to the client
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Email platform webhook (Mailchimp / ConvertKit / ActiveCampaign) — provided by client
EMAIL_WEBHOOK_URL=

# Booking/consultation page URL — provided by client
BOOKING_URL=

PORT=3000
